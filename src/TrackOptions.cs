namespace ADLMidi.NET
{
    /// <summary>
    /// Track options
    /// </summary>
    public enum TrackOptions
    {
        On = 1,  // Enabled track
        Off = 2, // Disabled track
        Solo = 3 // Solo track
    }
}
